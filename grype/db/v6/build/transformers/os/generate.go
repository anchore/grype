package os

// rhelMinorSpan (rhel_minor_span_generated.go) is generated from Red Hat's official
// product life-cycle API; see generate/main.go. Values change only when Red Hat
// announces a new major (or retires one), so most runs are a no-op. oss-release runs
// `make generate:rhel-versions` weekly and opens a PR when the data does move.
//go:generate go run ./generate
